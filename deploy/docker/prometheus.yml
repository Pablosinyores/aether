global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "aether-go"
    static_configs:
      - targets: ["aether-go:9090"]

  # aether-rust: no metrics endpoint yet — add scrape target when Rust exposes Prometheus metrics
